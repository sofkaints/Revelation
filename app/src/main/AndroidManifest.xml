<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="InnerclassSeparator"
    package="com.yenusoft.revelation">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_rev"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_rev_round"
        android:supportsRtl="true"
        android:theme="@style/Revel">
        <activity android:name=".Logo"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Lobby.Lobby"/>
        <activity android:name=".RevelationQuiz.RevelationQuiz"
            android:windowSoftInputMode="stateVisible"/>
    </application>

</manifest>